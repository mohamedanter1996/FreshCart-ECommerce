import { DecrementTitlePipe } from './decrement-title.pipe';

describe('DecrementTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new DecrementTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
